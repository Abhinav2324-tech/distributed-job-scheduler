{
  "installCommand": "npm install",
  "buildCommand": "npm run build --workspace @jobscheduler/shared && npm run build --workspace @jobscheduler/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
